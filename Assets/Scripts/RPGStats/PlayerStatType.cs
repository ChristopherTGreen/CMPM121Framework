using UnityEngine;

public class PlayerStats : MonoBehaviour
{
    public enum PlayerStatType
    {
        MaxHealth,
        SpellPower,
        ManaRegen,
        MaxMana,
        MoveSpeed
    }
}
